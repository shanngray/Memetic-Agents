{
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "Search the web for information when the agent needs external knowledge. Use this tool when you need to find current or factual information.",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Query to search the web with"}
            },
            "required": ["query"]
        }
    }
}
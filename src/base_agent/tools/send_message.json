{
  "type": "function",
  "function": {
    "name": "send_message",
    "description": "Send a message via API to another agent registered in the directory service",
    "parameters": {
      "type": "object",
      "properties": {
        "receiver": {
          "type": "string",
          "description": "The name of the receiving agent"
        },
        "content": {
          "type": "string",
          "description": "The message content to send"
        }
      },
      "required": ["receiver", "content"],
      "additionalProperties": false
    },
    "strict": true
  }
}
